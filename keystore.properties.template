# Copy this file to `keystore.properties` at the project root and fill the values.
# Do NOT commit the real keystore.properties file.

storeFile=C:\Users\YOU\keys\kusinai-release.jks
storePassword=your_store_password
keyAlias=kusinai_key
keyPassword=your_key_password
